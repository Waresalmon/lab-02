<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=" City List"
        android:textSize="36sp"
        android:textColor="#FFFFFF"
        android:textAlignment="textStart"
        android:background="#673AB7"
        android:paddingTop="10sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/city_list">
    </TextView>

    <ListView
        android:id="@+id/city_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/title"
        app:layout_constraintBottom_toTopOf="@id/deleteCityButton">

    </ListView>

    <Button
        android:id="@+id/addCityButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="ADD CITY"
        app:layout_constraintTop_toBottomOf="@id/city_list"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/deleteCityButton"
        app:layout_constraintWidth_percent="0.5">
    </Button>

    <Button
        android:id="@+id/deleteCityButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="DELETE CITY"
        app:layout_constraintTop_toBottomOf="@id/city_list"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/addCityButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.5">
    </Button>

    <EditText
        android:id="@+id/hiddenAddCityEditText"
        android:visibility="invisible"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:paddingBottom="25dp"
        android:text="AddCity"
        android:background="#6030B0"
        android:textAlignment="center"
        android:textColor="#F0F0F0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">
    </EditText>
    <Button
        android:id="@+id/cancel_button"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        app:layout_constraintStart_toStartOf="@id/hiddenAddCityEditText"
        app:layout_constraintBottom_toBottomOf="@id/hiddenAddCityEditText">
    </Button>
    <Button
        android:id="@+id/confirm_button"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Confirm"
        app:layout_constraintEnd_toEndOf="@id/hiddenAddCityEditText"
        app:layout_constraintBottom_toBottomOf="@id/hiddenAddCityEditText">

    </Button>




</androidx.constraintlayout.widget.ConstraintLayout>